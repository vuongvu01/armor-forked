name: CI.TOKENS

on:
    pull_request:
        branches:
            - dev
            - master
        paths:
            - 'packages/tokens/**'

jobs:
    test:
        runs-on: ubuntu-latest
        env:
            GITHUB_TOKEN: ${{ secrets.CICD_TOKEN }}

        steps:
            - uses: actions/checkout@v2

            - name: Install (common)
              run: yarn install

            - name: Build
              run: yarn run build
