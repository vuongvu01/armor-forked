/**
 * Use this file to define the <Switch /> component symbol that is open to the end consumers.
 */

import { SwitchPropsType as PropsType } from './type';

export * from './Switch';
export type SwitchPropsType = PropsType;
