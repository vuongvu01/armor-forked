/**
 * Use this file to define the list of <Tooltip /> component symbols that is open to the end consumers.
 */

import { TooltipPropsType as PropsType } from './type';

export * from './Tooltip';
export type TooltipPropsType = PropsType;
