import { shadow200, white } from '../../tokens';

export const paperDefaultTheme = {
    Root: {
        base: {
            backgroundColor: white,
            borderRadius: '4px',
            boxShadow: shadow200,
        },
    },
};
