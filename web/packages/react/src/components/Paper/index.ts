/**
 * Use this file to define the list of <Paper /> component symbols that is open to the end consumers.
 */

import { PaperPropsType as PropsType } from './type';

export * from './Paper';
export type PaperPropsType = PropsType;
