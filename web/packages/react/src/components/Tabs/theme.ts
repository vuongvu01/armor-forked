import { componentSpacing01 } from '../../tokens';

export const tabsDefaultTheme = {
    Root: {
        base: {
            backgroundColor: '$color.neutral.00',
            boxShadow: '$elevation.medium',
            marginBottom: componentSpacing01,
        },
    },
};
