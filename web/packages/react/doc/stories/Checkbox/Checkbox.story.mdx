import {useState} from 'react';
import { BrowserRouter, Link } from 'react-router-dom';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import { Checkbox, Button } from '../../../src/components';

<Meta title="Components/Checkbox" component={Checkbox} />

# Checkbox

~~~js
import { Checkbox } from '@deliveryhero/armor';
~~~

## States



<Preview>
    <Story name="States" height="220px">
    {() => {
       const [isChecked, setChecked] = useState(false);
       return (
 <>
     <Checkbox checked={isChecked} label={isChecked ? 'Checked' : 'Unchecked'} onChange={() => setChecked(!isChecked)} />
     <br />
     <br />
     <Checkbox defaultChecked label="Checked" />
     <br />
     <br />
     <Checkbox disabled label="Unchecked disabled" />
     <br />
     <br />
     <Checkbox checked disabled label="Checked disabled" />
     <br />
     <br />
     <Checkbox checked disabled checkedIcon="dash" label="Checked disabled dash"  />
 </>
    )
}}
    </Story>
</Preview>

## Checkmark styles

<Preview>
    <Story name="Checkmark styles" height="120px">
       <>
            <Checkbox defaultChecked checkedIcon="tick" label="tick" />
            <h1 />
            <Checkbox defaultChecked checkedIcon="dash" label="dash" />
        </>
    </Story>
</Preview>

## Properties

<Props of={Checkbox} />
