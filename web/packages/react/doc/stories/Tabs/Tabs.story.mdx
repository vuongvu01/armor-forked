import { useState } from 'react';
import { BrowserRouter, Link } from 'react-router-dom';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import get from 'lodash.get';

import { Box, Button, Tab, Tabs, TabView  } from 'src/components';

<Meta title="Components/Tabs" component={Tabs} />

# Tabs

```js
import { Tabs, Tab, TabView } from '@deliveryhero/armor';
```

## Default layout with minimum configuration

<Preview>
    <Story name="DefaultLayoutMinimumConfiguration" height="240px">
        {() => {
 const [selectedTabIndex, setSelectedTabIndex] = useState(0);
 const handleChange = (tabIndex) => setSelectedTabIndex(tabIndex);
 return (
     <>
         <Box
             padding={3}
             style={{
                 backgroundColor: '#F4F4F8',
                 height: '180px',
                 width: '600px',
             }}
         >
             <Tabs onSwitch={handleChange}>
                 <Tab label="Pizza"/>
                 <Tab label="Pasta"/>
                 <Tab label="Risotto"/>
                 <Tab label="Beverages"/>
                 <Tab label="Alcoholic beverages" disabled/>
             </Tabs>
             <TabView value={0} selectedValue={selectedTabIndex}>
                 <p>Pizza</p>
             </TabView>
             <TabView value={1} selectedValue={selectedTabIndex}>
                 <p>Pasta</p>
             </TabView>
             <TabView value={2} selectedValue={selectedTabIndex}>
                 <p>Risotto</p>
             </TabView>
             <TabView value={3} selectedValue={selectedTabIndex}>
                 <p>Beverages</p>
             </TabView>
             <TabView value={4} selectedValue={selectedTabIndex}>
                 <p>Alcoholic beverages</p>
             </TabView>
         </Box>
     </>
 );
}}
    </Story>
</Preview>

## Multiple tabs point to the content

<Preview>
    <Story name="MultipleTabsToSingleContent" height="240px">
        {() => {
 const [selectedTabIndex, setSelectedTabIndex] = useState(0);
 const handleChange = (tabIndex) => setSelectedTabIndex(tabIndex);
 return (
     <>
         <GroupHelper gap={2}>
             <Box
                 padding={3}
                 style={{
                     backgroundColor: '#F4F4F8',
                     height: '180px',
                     width: '600px',
                 }}
             >
                 <Tabs onSwitch={handleChange}>
                     <Tab label="Pizza" value={0} />
                     <Tab label="Pasta" value={0} />
                     <Tab label="Risotto" />
                     <Tab label="Beverages" value={0} />
                     <Tab label="Alcoholic beverages" disabled />
                 </Tabs>
                 <TabView value={0} selectedValue={selectedTabIndex}>
                     <p>
                         Pizza, Pasta and Beverages all point to the same
                         content
                     </p>
                 </TabView>
                 <TabView value={1} selectedValue={selectedTabIndex}>
                     <p>Pasta</p>
                 </TabView>
                 <TabView value={2} selectedValue={selectedTabIndex}>
                     <p>Risotto has a content of its own</p>
                 </TabView>
                 <TabView value={3} selectedValue={selectedTabIndex}>
                     <p>Beverages</p>
                 </TabView>
                 <TabView value={4} selectedValue={selectedTabIndex}>
                     <p>Alcoholic beverages</p>
                 </TabView>
             </Box>
         </GroupHelper>
     </>
 );
}}
    </Story>
</Preview>

## Full width

<Preview>
    <Story name="FullWidth" height="240px">
        {() => {
 const [selectedTabIndex, setSelectedTabIndex] = useState(0);
 const handleChange = (tabIndex) => setSelectedTabIndex(tabIndex);
 return (
     <>
         <GroupHelper gap={2}>
             <Box
                 padding={3}
                 style={{
                     height: '180px',
                     backgroundColor: '#F4F4F8',
                 }}
             >
                 <Tabs fullWidth onSwitch={handleChange}>
                     <Tab label="Pizza" value={1} />
                     <Tab label="Pasta" value={2} />
                 </Tabs>
                 <TabView value={0} selectedValue={selectedTabIndex}>
                     <p>Pizza</p>
                 </TabView>
                 <TabView value={1} selectedValue={selectedTabIndex}>
                     <p>Pasta</p>
                 </TabView>
                 <TabView value={2} selectedValue={selectedTabIndex}>
                     <p>Risotto</p>
                 </TabView>
                 <TabView value={3} selectedValue={selectedTabIndex}>
                     <p>Beverages</p>
                 </TabView>
                 <TabView value={4} selectedValue={selectedTabIndex}>
                     <p>Alcoholic beverages</p>
                 </TabView>
             </Box>
         </GroupHelper>
     </>
 );
}}
    </Story>
</Preview>

## `Tabs` Properties

<Props of={Tabs} />

## `Tab` Properties

<Props of={Tab} />

## `TabView` Properties

<Props of={TabView} />
