import { addParameters } from '@storybook/react';

addParameters({
    docs: {
        inlineStories: false,
        previewSource: 'open',
    },
    options: {
        isToolshown: false,
        showNav: true,
    },
});
