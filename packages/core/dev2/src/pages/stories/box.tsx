import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/Box/__story__/Box.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
