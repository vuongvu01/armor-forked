import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/IconButton/__story__/IconButton.dev.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
