import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/ClearButton/__story__/ClearButton.dev.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
