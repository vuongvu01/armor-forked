import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/FileUploader/__story__/FileUploader.dev.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
