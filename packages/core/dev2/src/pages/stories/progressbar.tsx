import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/ProgressBar/__story__/ProgressBar.dev.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
