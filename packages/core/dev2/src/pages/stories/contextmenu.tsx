import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/ContextMenu/__story__/ContextMenu.dev.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
