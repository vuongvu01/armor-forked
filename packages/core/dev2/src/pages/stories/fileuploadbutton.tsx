import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/FileUploadButton/__story__/FileUploadButton.dev.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
