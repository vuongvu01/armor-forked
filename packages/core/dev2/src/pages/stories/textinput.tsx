import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/TextInput/__story__/TextInput.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
