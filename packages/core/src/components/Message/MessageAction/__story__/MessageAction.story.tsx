import React from 'react';

import { MessageAction } from '../MessageAction';

export default {
    title: 'Components/MessageAction',
    component: MessageAction,
    parameters: {},
};

export const Basic = () => <MessageAction>Hello world!</MessageAction>;
