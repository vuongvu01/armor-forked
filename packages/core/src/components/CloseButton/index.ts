/**
 * Use this file to define the list of <CloseButton /> component symbols that is open to the end consumers.
 */
import { CloseButtonPropsType as PropsType } from './type';

export * from './CloseButton';
export type CloseButtonPropsType = PropsType;
