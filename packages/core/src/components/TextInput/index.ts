/**
 * Use this file to define the list of <TextInput /> component symbols that is open to the end consumers.
 */

import { TextInputPropsType as PropsType } from './type';

export * from './TextInput';
export type TextInputPropsType = PropsType;
