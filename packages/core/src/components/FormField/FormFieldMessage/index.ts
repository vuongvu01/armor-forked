/**
 * Use this file to define the list of <FormFieldMessage /> component symbols that is open to the end consumers.
 */

import { FormFieldMessagePropsType as PropsType } from './type';

export * from './FormFieldMessage';
export type FormFieldMessagePropsType = PropsType;
