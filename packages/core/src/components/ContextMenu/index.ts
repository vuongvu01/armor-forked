/**
 * Use this file to define the list of <ContextMenu /> component symbols that is open to the end consumers.
 */
import { ContextMenuPropsType as PropsType } from './type';

export * from './ContextMenu';
export type ContextMenuPropsType = PropsType;
