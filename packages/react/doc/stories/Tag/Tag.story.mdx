import {useState} from 'react';
import {Meta, Preview, Props, Story} from '@storybook/addon-docs/blocks';

import {Tag} from 'src/components';

<Meta title="Components/Tag" component={Tag} />

```js
import { Tag } from '@deliveryhero/armor';

<Tag label="Default" />
<Tag type="new" />
```

## User Tag

<Preview>
    <Story name="UserTag" height="300px">
        <>
            <GroupHelper gap={2}>
                <p>Delete button appears onHover</p>
                <Tag label="Default" />
            </GroupHelper>
            <GroupHelper gap={2}>
                <p>Delete button always enabled</p>
                <Tag
                    label="Default"
                    deleteOption="enabled"
                />
            </GroupHelper>
            <GroupHelper gap={2}>
                <p>Delete button always disabled</p>
                <Tag label="Default" deleteOption="disabled" />
            </GroupHelper>
        </>
    </Story>
</Preview>

## Status Tags

<Preview>
    <Story name="StatusTags" height="140px">
        <>
            <GroupHelper gap={2}>
                <Tag type="approved" />
                <Tag type="denied" />
                <Tag type="new" />
            </GroupHelper>
        </>
    </Story>
</Preview>

## Interactive Example

<Preview>
    <Story name="InteractiveExample" height="140px">
        {() => {
const [isOpen1, setIsOpen1] = useState(true);
const [isOpen2, setIsOpen2] = useState(true);
return (
    <>
        <GroupHelper gap={2}>
            {isOpen1 && (
                <Tag
                    label="Close me"
                    deleteOption="enabled"
                    onClose={() => setIsOpen1(false)}
                />
            )}
            {isOpen2 && (
                <Tag label="Close me" onClose={() => setIsOpen2(false)} />
            )}
        </GroupHelper>
    </>
);
        }}
    </Story>
</Preview>

## `Tag` Properties

<Props of={Tag} />
