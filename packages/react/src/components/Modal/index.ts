/**
 * Use this file to define the list of <Modal /> component symbols that is open to the end consumers.
 */

import { ModalPropsType as PropsType } from './type';

export * from './Modal';
export type ModalPropsType = PropsType;
