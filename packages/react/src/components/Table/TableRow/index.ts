/**
 * Use this file to define the list of <TableRow /> component symbols that is open to the end consumers.
 */

import { TableRowPropsType as PropsType } from './type';

export * from './TableRow';
export type TableRowPropsType = PropsType;
