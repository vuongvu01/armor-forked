import { makeTheme } from '../../../../styling';

export const customTheme = makeTheme({
    armor: {
        components: {
            TableExpandableSection: {
                borderWidth: '2px',
            },
        },
    },
});
