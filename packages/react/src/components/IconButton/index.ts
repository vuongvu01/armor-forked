/**
 * Use this file to define the list of <IconButton /> component symbols that is open to the end consumers.
 */
import { IconButtonPropsType as PropsType } from './type';

export * from './IconButton';
export type IconButtonPropsType = PropsType;
