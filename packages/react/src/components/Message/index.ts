/**
 * Use this file to define the list of <Message /> component symbols that is open to the end consumers.
 */

import { MessagePropsType as PropsType } from './type';

export * from './Message';
export type MessagePropsType = PropsType;
