/**
 * Use this file to define the list of <PageNavigation /> component symbols that is open to the end consumers.
 */

import { PageNavigationPropsType as PropsType } from './type';

export * from './PageNavigation';
export type PageNavigationPropsType = PropsType;
