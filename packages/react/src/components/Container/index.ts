/**
 * Use this file to define the list of <Container /> component symbols that is open to the end consumers.
 */
import { ContainerPropsType as PropsType } from './type';

export * from './Container';
export type ContainerPropsType = PropsType;
