/**
 * Use this file to define the list of <Card /> component symbols that is open to the end consumers.
 */
import { CardPropsType as PropsType } from './type';

export * from './Card';
export type CardPropsType = PropsType;
