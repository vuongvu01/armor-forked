/**
 * Use this file to define the list of <Indicator /> component symbols that is open to the end consumers.
 */

import { IndicatorPropsType as PropsType } from './type';

export * from './Indicator';
export type IndicatorPropsType = PropsType;
