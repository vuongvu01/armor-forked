import React from 'react';

import { StoryPageLayout } from '../../components';
import * as Stories from '../../armor/components/Tabs/__story__/Tabs.story';

export default () => {
    return <StoryPageLayout stories={Stories} />;
};
