import React, {  ReactElement } from 'react';
import styled from 'styled-components';

import * as I from '..';

export default {
    title: 'generatedStories',
    parameters: {},
};

const Illustrations = styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
`;

const Container = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
`;

const Illustration = styled.div`
    border-radius: 2px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.5rem;
    background-color: #f7f7f7;
    color: #050505;
`;

export const AllCategories = (): ReactElement => (
    <>
      <% categories.forEach(function(category) { %>
        <h2><%- category.name %></h2>

        <Illustrations>
            <% category.illustrations.forEach(function(illustration) { %>
              <Container>
                <Illustration>
                  <I.<%- illustration %>Illustration width="100px" />
                </Illustration>
                <h3><%- illustration %></h3>
              </Container>
            <% }); %>
        </Illustrations>
      <% }); %>
    </>
);
